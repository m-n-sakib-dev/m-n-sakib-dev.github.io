/** @type {import('next').NextConfig} */
const nextConfig = {
        output: "export",
        basePath: "/m-n-sakib-dev.github.io",
        images: {
                unoptimized: true,
        },
};

export default nextConfig;
